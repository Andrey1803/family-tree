fix: skip spouse ancestors when collecting related persons

Added skipSpouseAncestors parameter to collect() function.
When collecting spouses, we now pass skipSpouseAncestors=true
to prevent adding their ancestors, reducing line crossings.

This matches desktop version behavior where spouse ancestors
are collapsed by default.
