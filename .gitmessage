fix: restore proper recursive layout algorithm

Moved the coords[pid] check to the beginning of layout() and removed
the early return condition for parents. Now layout() properly traverses
the entire tree recursively from rootId, placing all persons correctly.

This matches the desktop version's place_subtree() logic.
