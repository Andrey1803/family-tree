# Railway / Nixpacks: сборка веб-приложения «Семейное древо»
# https://docs.railway.com/guides/flask

[phases.setup]
nixPkgs = ["python312"]

[phases.install]
skip = true
cmds = ["pip install -r web/requirements.txt"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT web.app:app"
