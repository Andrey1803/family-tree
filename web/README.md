# Семейное древо — Web-версия

Веб-версия приложения «Семейное древо». Использует те же данные (users.json, family_tree_*.json), что и desktop-версия.

📁 **Основная документация:** [README.md](../README.md) в корне проекта

---

## Как запустить веб-версию

### Шаг 1. Установить Python

Убедитесь, что на компьютере установлен Python 3.8 или новее. Проверить:

```bash
python --version
```

Или (если основная команда — `python3`):

```bash
python3 --version
```

### Шаг 2. Перейти в папку проекта

Откройте терминал (командную строку) и перейдите в папку, где лежит проект. Например:

```bash
cd "D:\Мои документы\Рабочий стол\hobby\Projects\Дерево"
```

### Шаг 3. Установить зависимости

Перейдите в папку `web` и установите библиотеки Flask:

```bash
cd web
pip install -r requirements.txt
```

Если основной интерпретатор — `python3`, используйте:

```bash
pip3 install -r requirements.txt
```

### Шаг 4. Запустить сервер

Выполните:

```bash
python app.py
```

Или:

```bash
python3 app.py
```

Увидите сообщение вроде:

```
 * Running on http://127.0.0.1:5000
 * Debug mode: on
```

### Шаг 5. Открыть в браузере

Откройте браузер и перейдите по адресу:

**http://127.0.0.1:5000**

Или: **http://localhost:5000**

### Шаг 6. Войти в систему

- **Войти** — логин и пароль (если есть учётная запись)
- **Регистрация** — создание нового пользователя
- **Войти без регистрации** — режим «Гость» (дерево `family_tree_Гость.json`)

---

## Остановка сервера

В терминале, где запущен сервер, нажмите **Ctrl+C**.

---

## Возможности

- **Авторизация**: вход, регистрация, гость
- **Просмотр дерева**: ортогональные линии родители–дети, линии между супругами
- **Навигация**: клик по карточке — выбор центра; перетаскивание (pan); зум колёсиком мыши
- **Контекстное меню** (правый клик по карточке):
  - Просмотреть — информация о персоне
  - Сделать центром
  - Редактировать — имя, отчество, фамилия, даты, пол, умер(ла)
  - Удалить
  - Родственник: Отец, Мать, Сын, Дочь, Брат, Сестра, Супруг(а)
- **Добавление первой персоны** при пустом дереве

## Скачивание desktop-версии

Кнопка «Установить приложение» на странице дерева отдаёт ZIP с исходниками (main.py + папка Дерево). Исходники лежат в `web/desktop_bundle/`. После изменений в desktop-приложении обновите bundle:

```bash
python web/sync_desktop_bundle.py
```

## Совместимость

Данные хранятся в том же формате, что и в desktop-версии. Дерево, созданное в desktop, отображается в web и наоборот.

## Если что-то не работает

- **«python не найден»** — установите Python с https://www.python.org/downloads/
- **«pip не найден»** — используйте `python -m pip install -r requirements.txt`
- **Ошибка при запуске** — проверьте, что вы в папке `web` и файл `app.py` там есть
